export const ContactUs = (req, res) => {
  res.json({ message: "Contact-Us successful from controller" });
};

export const Feedback = (req, res) => {
  res.json({ message: "Feedback successful from controller" });
};
